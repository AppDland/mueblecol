const Loading = () => {

    return (
        <div className="fixed bg-black w-full h-full flex justify-center items-center z-50 top-0 select-none">
            <p className="text-white">Cargando...</p>
        </div>
    )
}

export default Loading;